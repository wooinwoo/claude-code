{
  "mcpServers": {
    "github": {
      "command": "node",
      "args": [".claude/scripts-wiw/run-github-mcp.cjs"],
      "description": "GitHub - PR, issue, repo 관리 (.claude/.env에서 GITHUB_PAT 로드)"
    },
    "mcp-atlassian": {
      "command": "node",
      "args": [".claude/scripts-wiw/run-jira-mcp.cjs"],
      "description": "Jira - 이슈 조회/생성 (.claude/.env에서 JIRA_TOKEN 로드)"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "description": "세션 간 영구 메모리"
    },
    "context7": {
      "command": "npx",
      "args": ["-y", "@context7/mcp-server"],
      "description": "라이브 문서 조회 (npm, 프레임워크 docs)"
    },
    "magic": {
      "command": "npx",
      "args": ["-y", "@magicuidesign/mcp@latest"],
      "description": "Magic UI 컴포넌트"
    }
  },
  "_usage": {
    "setup": "프로젝트 .claude.json 또는 settings.local.json의 mcpServers에 필요한 서버를 복사",
    "env": ".claude/.env 파일에 GITHUB_PAT, JIRA_TOKEN, JIRA_URL, JIRA_USERNAME 설정",
    "tip": "MCP 서버는 10개 이하로 유지 (컨텍스트 윈도우 절약)"
  }
}
